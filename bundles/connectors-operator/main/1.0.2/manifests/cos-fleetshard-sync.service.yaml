apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: cos-fleetshard-sync
  name: cos-fleetshard-sync
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app.kubernetes.io/name: cos-fleetshard-sync
  type: ClusterIP
